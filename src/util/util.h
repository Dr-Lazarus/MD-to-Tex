#ifndef NODE_H
#define NODE_H
#include "../node.h"
#endif
#ifndef TREE_H
#define TREE_H
#include "../tree.h"
#endif

void add_text_data(md_node *node, const char *line, int line_length,
                   char delim);
